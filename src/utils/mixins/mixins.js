/**
 * @description: mixins混入，可以让多个组件使用同一个js逻辑
 * @return {*}
 */
export const mixin={
    components:{},
    data () {
        return {
            
        }
    },
    created () {},
    mounted () {},
    computed:{},
    methods:  {},
}